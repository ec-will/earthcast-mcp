# Earthcast MCP Server Environment Variables
#
# This server provides access to environmental and weather data from:
# - Earthcast Technologies API (http://ect-sandbox.com → https://ect-api.com)
#
# All environment variables are OPTIONAL with sensible defaults.
# Copy this file to .env and customize as needed.

# ============================================================================
# API CONFIGURATION
# ============================================================================
# Earthcast Technologies API URL
# Default: http://ect-sandbox.com (development/testing)
# Production: https://ect-api.com (when available)
#ECT_API_URL=http://ect-sandbox.com

# API Authentication (HTTP Basic Auth)
# Required for API access
#ECT_API_USERNAME=your_username
#ECT_API_PASSWORD=your_password

# ============================================================================
# CACHE CONFIGURATION
# ============================================================================
# Enable/disable in-memory caching (default: true)
# Caching reduces API calls by 50-80% and improves response times
CACHE_ENABLED=true

# Maximum number of entries in cache before LRU eviction (default: 1000)
# Range: 100-10000
# Higher values = more memory usage but better cache hit rates
CACHE_MAX_SIZE=1000

# API request timeout in milliseconds (default: 30000 = 30 seconds)
# Range: 5000-120000 (5 seconds to 2 minutes)
# Increase if you experience timeout errors on slow connections
#API_TIMEOUT_MS=30000

# ============================================================================
# TOOL CONFIGURATION
# ============================================================================
# Control which MCP tools are exposed
# Default: 'basic'
# TODO: Define tool presets for Earthcast API
#ENABLED_TOOLS=basic

# ============================================================================
# LOGGING
# ============================================================================
# Log level: 0=DEBUG, 1=INFO, 2=WARN, 3=ERROR (default: 1)
# Lower values = more verbose logging
LOG_LEVEL=1


# ============================================================================
# NOTES
# ============================================================================
# - All settings have sensible defaults and can be omitted
# - The ECT_API_URL will automatically switch to production when ect-api.com is available
# - Environment variables can also be set in your MCP client configuration
